# Architecture

> Placeholder — to be filled in by the team.

## Overview

Data flow: source adapters → ingestion (provenance stamping) → profile builder
→ embeddings + FAISS vector store → deterministic match engine → agent runtime
(cited rationales, with Track B fallback) → FastAPI `POST /match`.

## Components

## Data flow

## Deployment (Docker → AWS)

## Track A / Track B

## Decisions
